# Lots 4-5-6 — Projets/Templates, Process/Tools, Git (Core portable + tests)

À déposer sur `avalonia` (cumulatif sur les lots 0-3). Aucun changement de .csproj
nécessaire (System.Diagnostics.Process fait partie du framework).

## Lot 4 — Projets & templates (filesystem, sans processus)
```
Core/Models/GamebuinoProject.cs      # migré (namespace) : détection PIO/ESP-IDF, .aka-build
Core/Services/IProjectService.cs     # NEW
Core/Services/ProjectService.cs      # migré : dépend d'ISettingsService, scan workspace
Core/Services/ITemplateService.cs    # NEW
Core/Services/TemplateService.cs     # migré : génère PlatformIO / ESP-IDF (coquille) en code
```
Tests : scan détecte PIO + ESP-IDF (et ignore le reste), suppression, création
d'un projet PlatformIO (platformio.ini + main.cpp) et d'un projet ESP-IDF
(CMakeLists + main/app_main + .aka-build=espidf + placeholder lib).

## Lot 5 — Exécution de processus & détection d'outils
```
Core/Models/ProcessResult.cs         # record (ExitCode, Stdout, Stderr, Success)
Core/Platform/IProcessRunner.cs      # ProcessRequest (args SÉPARÉS) + RunAsync / RunStreamingAsync
Core/Platform/SystemProcessRunner.cs # ArgumentList (espaces gérés), pas de shell
Core/Platform/ToolLocator.cs         # IToolLocator + impl : chemin configuré → PATH → emplacements connus
```
> **Écart assumé au plan** : `IProcessRunner`/`IToolLocator` **et** leurs impls
> sont dans **Core** (pur .NET, zéro Avalonia) pour être testables en CI. App
> n'aura qu'à les enregistrer en DI. La détection d'OS (`OperatingSystem.IsWindows`)
> reste localisée dans ces classes ; les ViewModels ne feront jamais de Process.Start.

Tests : `dotnet --version` (RunAsync + streaming), exécutable introuvable → lève ;
locate sur PATH, inconnu → null, chemin configuré prioritaire, chemin configuré
invalide → repli PATH.

## Lot 6 — GitService
```
Core/Services/IGitService.cs         # NEW
Core/Services/GitService.cs          # NEW : localise « git » (IToolLocator), exécute via IProcessRunner
```
- Plus de `git.exe` en dur, plus de commande concaténée : `["clone", url, dest]`.
- Tests (runner + locator factices) : arguments séparés, espaces préservés dans
  la destination, workspace absent → lève, destination existante → lève, git
  introuvable → lève, ExtractRepoName (plusieurs URLs).

## Vérif
`dotnet test` doit maintenant afficher ~**35 tests** (16 des lots 0-3 + ceux-ci).
Les tests de Lot 5 lancent un vrai `dotnet` (présent dès qu'on exécute `dotnet test`).

## Câblage App (plus tard, racine de composition)
```csharp
var paths  = PlatformPathsFactory.Create();
var runner = new SystemProcessRunner();
var tools  = new ToolLocator();
var settings = new SettingsService(paths);
var git    = new GitService(settings, runner, tools);
var projects = new ProjectService(settings);
var templates = new TemplateService(settings);
```

## Reste hors Core (lot suivant)
PlatformIOService / BuildService / EspIdfService → à porter en Lot 7 sur
IProcessRunner + IToolLocator (mêmes principes). VSCode/launcher → Lot 8.
